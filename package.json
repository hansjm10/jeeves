{
  "name": "jeeves",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint \"{apps,packages}/**/*.{ts,tsx,js,jsx}\" --max-warnings=0",
    "typecheck": "tsc -b --pretty false",
    "build": "tsc -b --pretty false",
    "test": "vitest run",
    "run:fixture": "pnpm exec jeeves-runner run-fixture",
    "dev:viewer-server": "pnpm build && node apps/viewer-server/dist/bin.js --host 127.0.0.1 --port 8081",
    "dev:viewer": "pnpm --filter @jeeves/viewer dev",
    "dev": "pnpm build && concurrently -k -n viewer,viewer-server -c cyan,magenta \"pnpm dev:viewer\" \"node apps/viewer-server/dist/bin.js --host 127.0.0.1 --port 8081\"",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@jeeves/cli": "workspace:*",
    "@jeeves/runner": "workspace:*",
    "@types/node": "^25.1.0",
    "concurrently": "^9.2.1",
    "eslint": "^9.39.2",
    "lefthook": "^2.0.16",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.54.0",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@10.28.2"
}
