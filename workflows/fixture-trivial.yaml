workflow:
  name: fixture-trivial
  version: 2
  start: hello

phases:
  hello:
    prompt: fixtures/trivial.md
    type: execute
    description: Fixture phase for runner integration tests
    transitions:
      - to: complete
        auto: true

  complete:
    type: terminal
    description: Fixture complete

